import { LitElement } from 'lit-element';
export * from 'lit-element';
export declare class WiredBase extends LitElement {
    fireEvent(name: string, detail?: any, bubbles?: boolean, composed?: boolean): void;
}
